<?php require_once 'templates/Header.phtml'; ?>

<div class="d-grid gap-2 col-6 mx-auto">
  <?php if ($isAdmin) { ?>
    <a href="<?= BASE_URL . 'FormAgregarCliente/'; ?>" class="btn btn-success btn-sm">Agregar Cliente</a>
  <?php } ?>
</div>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Nombre del Cliente</th>
      <th>Ver Detalle</th>
      <?php if ($isAdmin) { ?>
        <th>Opciones</th>
      <?php } ?>
    </tr>
  </thead>
  <tbody>
    <?php foreach ($clientes as $cliente) { ?>
      <tr>
        <td><?= $cliente->nombre; ?></td>
        <td><a href="<?= BASE_URL . 'ClienteDetalles/' . $cliente->id_cliente; ?>" class="btn btn-primary btn-sm">Ver Detalle</a></td>
        <?php if ($isAdmin) { ?>
          <td>
            <a href="<?= BASE_URL . 'FormActualizarCliente/' . $cliente->id_cliente; ?>" class="btn btn-warning">Editar</a>
            <a href="<?= BASE_URL . 'EliminarCliente/' . $cliente->id_cliente; ?>" class="btn btn-danger">Eliminar</a>
          </td>
        <?php } ?>
      </tr>
    <?php } ?>
  </tbody>
</table>

<?php require_once 'templates/Footer.phtml'; ?>